# 首届教学智能体大赛 - 执行计划（v2：按学生端统一产品地图重写）

> 基于 [教学智能体方案讨论.md](教学智能体方案讨论.md) 第八、九节、[智能体开发后台工具清单.md](智能体开发后台工具清单.md)、[知识图谱.md](知识图谱.md)、[产品设计图/](产品设计图/) 整理
> 记录时间：2026-08-07 首版；2026-08-21 更新知识库范围+知识图谱+参数拖动交互技术方案实测；**2026-08-23 按"学生端统一产品地图"（四条车道）+"学生记忆系统"（信号提炼，不止做题）重写阶段四/五**；**2026-09-12 新增教学风格个性化方案1（学生显式调整），提示词库建立见 [提示词库.md](提示词库.md)，教材配图从docx提取完成待接入**；**2026-09-17 读官方通知原文 [首届教学智能体大赛通知.pdf](首届教学智能体大赛通知.pdf) 修正此前记错的截止日期，并补全材料/赛道要求**
> 目标：机械原理**全书**（教材第一~十八章，见 `教材/` 文件夹）"学习赋能+教学效果评估"融合智能体
> **⚠️ 截止日期更正（2026-09-17）**：此前一直记的"2026-09-21"是错的，官方通知原文（[首届教学智能体大赛通知.pdf](首届教学智能体大赛通知.pdf) 附件1"整体方案"）白纸黑字写的是**报名和参赛材料提交截止时间为 2026年10月10日 17:00**，以这份PDF为准，之前"只剩4天"的紧迫判断是错的，实际还有约3周
> **范围说明**：知识库覆盖全书18章内容；任务流/诊断题/变式题等交互逻辑仍按 MVP 思路**先打通第三章（平面连杆机构）**，跑通后再横向扩展到其他章节——知识库范围和交互功能范围是两件事，不要混为一谈
> 每一步都标注了在 robot.chaoxing.com 后台的**具体菜单路径**，方便直接照着操作

> **✅ 已确定目标课程**：机械设计基础A2（25-26-1），courseId `254589279`，张中利-铸造231-232塑型231班（clazzId `126556119`），系统已配置知识点"平面连杆机构"，是目前唯一验证到已有对齐知识点体系的班级

## 这次重写改了什么（先看这里，避免对着旧版本继续开发）

- **阶段四不再是"一条诊断→变式题的线性流程"，而是四条车道**：A报错求助 / B主动学习 / C自测复习 / D可视化探索，四条车道共享同一个"个性化层"（读取学生掌握度画像）和同一套"记忆写回"机制，具体见下方 [产品设计图/机械小助手_学生端统一产品地图.png](产品设计图/机械小助手_学生端统一产品地图.png)
- **数据沉淀的出发点从"做题对错"改成"对话信号"**：车道A/B/C/D 每一条都要在自己的判断/回复节点里顺带输出学习信号并写回学生档案，不再是只有做题相关的车道（原A/C）才产生数据，详见下方"学生记忆系统"整节（**2026-08-23更新**：原计划是单独拆一个"信号提炼节点"，实测确认可以合并进各车道已有的判断节点一次输出，见3.2/4.0）
- **阶段五教师端按 [机械小助手_教师端能力地图.png](产品设计图/机械小助手_教师端能力地图.png) 收窄范围**：只做"课堂演示"（复用车道D）+"学情画像自动推送"（触发器+Skill），"教师主动查询"明确排除在这版 MVP 外
- 阶段一/二/三（报名流程、内容准备、平台能力实测结论）**基本不受影响，保留原有已验证内容**，本次重写不动这几节的已确认事实

## 常用链接（当前项目：机械小助手）

| 用途 | 地址 |
|---|---|
| **开发后台**（基础调试/资源管理等，需登录） | `https://robot.chaoxing.com/dashboard/baseSetting?unitId=2339&robotId=971c00744366497c8b734834c88fa119` |
| **体验链接·新版**（大概率是报名要填的体验链接） | `https://robot.chaoxing.com/coze?unitId=2339&robotId=971c00744366497c8b734834c88fa119` |
| 体验链接·简版 | `https://robot.chaoxing.com/prime?unitId=2339&robotId=971c00744366497c8b734834c88fa119` |
| 体验链接·旧版（不再更新，不建议用） | `https://robot.chaoxing.com/chat?unitId=2339&robotId=971c00744366497c8b734834c88fa119` |
| 智雅项目管理入口 | `https://zhiya.chaoxing.com/fable-ans-vue/personalWorkbench/projectManage` |
| 大赛报名官网 | `https://tac.mh.chaoxing.com/` |

> unitId=2339（河南科技大学），robotId=971c00744366497c8b734834c88fa119（机械小助手项目）

## 总体思路（对应产品设计图·总览）

不再需要跳去 Coze 等外部平台开发，直接在 **robot.chaoxing.com**（智雅→项目管理→新建项目→AI创建，生成后跳转的后台）内搭建。核心不是"把诊断→反馈→验证这一条链路串起来"，而是把**一个统一入口，收敛成四条车道，最后汇总回同一份学生档案**这套结构，用【任务流】节点实现：

```
① 学生打开对话框，输入一句话
        ↓
② 意图路由（大模型生成节点）判断属于四类中的哪一类
        ↓
   ┌──────────┬──────────┬──────────┬──────────┐
   │ 车道A     │ 车道B     │ 车道C     │ 车道D     │
   │ 报错求助  │ 主动学习  │ 自测复习  │ 可视化探索│
   └──────────┴──────────┴──────────┴──────────┘
        ↓（四条车道都先经过同一层）
③ 共享个性化层：读取【数据源-表单】里"学生掌握度画像表"（表B），决定内容深度/讲法/题目难度
        ↓
   车道各自的固定任务流节点 + 模型自由生成的苏格拉底追问（分层见阶段四）
        ↓（车道自己的判断/回复节点顺带输出，不再单独占一个节点——2026-08-23实测确认可合并）
④ 车道判断节点在给出反馈的同时，一并输出这轮对话的学习信号，写入"学习信号记录表"（表A），
   再滚动更新表B的掌握状态
        ↓
⑤ 统一输出：回应学生

【教师端】不是第五条车道，而是复用车道D的嵌入组件（课堂演示）+ 触发器定时读表A/B生成班级画像（学情推送）
```

固定任务流节点 vs 模型自由生成的分层原则、以及为什么这么分层，见 [教学智能体方案讨论.md 9.1](教学智能体方案讨论.md) 和 9.3"关键设计判断"。

---

## 阶段一：报名与基础信息

**工具**：智雅平台（zhiya.chaoxing.com）个人中心，不涉及 robot.chaoxing.com

> **2026-09-17 读官方通知PDF后补充**：之前这节的步骤是按记忆写的，跟官方通知原文（附件2"普通高校赛道实施方案"、附件4"申报书"、附件5"建设说明书"）对照后发现遗漏了不少材料要求，以下已按PDF原文补全。河南科技大学是普通本科院校，走**普通高校赛道**（不是职业院校赛道）。

- [ ] 登录 `tac.mh.chaoxing.com`，【个人中心】→ 完成参赛承诺书签订
- [ ] 【报名信息】模块填写个人/团队信息（团队≤4人，1名负责人+不超过3名团队教师，每位教师/团队限报1项目，同一位教师不得加入多个团队），页面最下方"暂存"或"提交"
- [ ] **赛道与组别选择（原计划完全没提，PDF附件2"五、参赛组别"明确列出6个组别）**：机械小助手最贴合的是**"学习赋能智能体"**组别（原文：面向学生个性化学习需求，设计学情分析、答疑辅导、学习路径规划、错题复盘、技能训练、学习激励等智能体，实现因材施教）——这跟项目目前"四条车道+表A/B掌握度画像"的整体设计方向是对上的，文案措辞可以直接呼应这个组别的官方表述
- [ ] 报名表单里有几个下拉框需要实际登录后才能看清具体选项，逐一确认并截图记录：
  - "参赛渠道"下拉框
  - "智能体类别"下拉框（对应上面的6个参赛组别）
  - "专业大类""所属学段""应用场景"下拉框
  - 这几项的选择会决定我们最终提交材料里怎么描述项目定位，建议先确认选项范围再定文案措辞
- [ ] 【报名信息】页面点"申报书"按钮导出 → 在"下载中心"查看生成状态（有"正在生成""可下载"两种状态）→ 下载
- [ ] 申报书打印、**盖三处章**（PDF附件4显示申报书需要"学校教务部门意见"盖章 + "学校政治审查意见"盖章 + "学校意见"盖章，三个不同的公章/审批环节，不是盖一次章就行，找学校对应部门可能要花时间，建议提前联系）、扫描后，回到【我的作品】模块按"上传申报书"步骤上传
- [ ] **⚠️ 新发现：建设说明书（原计划完全没提这份材料，PDF附件5单独列出）**：在大赛官网填写，分两块——"简介"（开发背景/拟解决问题/核心功能/技术路线/创新点，**限1200字**）+ "应用成效"（课堂教学应用场景和实践成果，**限1800字**），另外要填测试链接/二维码/测试账号，还有"应用安全"勾选项（网络安全等级保护备案表/网络安全等级测评报告/生成式人工智能服务备案编号/生成式人工智能服务安全评估报告——若没有可以不勾，PDF原文写"无需附件提交"）
- [ ] **⚠️ 新发现：教学智能体展示视频（原计划完全没排期这项工作，且是评审50%权重的现场评审之外、网络评审阶段就要交的材料）**：时长6-10分钟、MP4格式、720P及以上、大小≤1200M，命名规则是"教学智能体名称"。需要完整呈现建设思路/核心功能/使用流程/应用场景/实际成效。这是目前执行计划里完全没有排期的一项工作，要单独规划录制/剪辑时间
- [ ] **⚠️ 新发现：全套材料强制匿名要求（PDF原文多处强调，此前完全没考虑）**：申报书之外的建设说明书、展示视频、现场汇报，**均不得出现参赛教师姓名、所在高校及院系名称等透露个人身份的信息**——建设说明书文案、视频里的截图/口播，都要过一遍这条红线（比如现在server.py日志、演示截图里如果带学校/姓名相关内容要注意脱敏）
- [ ] **⚠️ 新发现：原创性佐证材料需要教务盖章（PDF附件2"四、（五）佐证材料"）**：技术路线图、建设过程记录、应用案例、教学成效数据、师生反馈等佐证材料，"须经学校教务管理部门审核并加盖公章后上传"——这一步跟申报书盖章一样依赖学校流程，建议合并一次性走完，别分两次找教务部门
- [ ] **确认：涉及学生数据需脱敏**——这条跟项目里表A/B已经用学号（`INNER_userId`）而非真实姓名存储是对得上的，不需要额外改造，但材料/视频里展示学生数据截图时要留意
- [ ] 网络评审阶段"专家评审团协同AI对提交材料进行线上评审"（PDF原文），说明材料的完整性/规范性/表述清晰度本身就是评分项之一（见评分标准"展示与推广-材料完整性与规范性"15分里的一部分），不是随便糊一份交上去就行

## 阶段二：内容准备

**工具**：本地文档（`教材/` 全书18章docx，含部分章节图版），不依赖平台，可与阶段三并行

> 知识点/诊断题/变式题设计仍先聚焦第三章（见下），其余章节内容已有 [知识图谱.md](知识图谱.md) 拆解结果，留作后续横向扩展任务流时使用。这些题目和讲解话术是**四条车道共用的内容资产**，不是只服务车道A：车道A/C 用来出诊断题/自测题，车道B 用来做讲解深度分层的素材，车道D 用来在拖动模拟器时结合数据讲解。

- [ ] 把机械原理第三章内容拆成6个知识点节点（每个节点对应【任务流】里的一个分支/意图）：
  1. 杆长条件判断
  2. 整转副位置判断（最短杆为连架杆/机架/连杆）
  3. 三种机构类型判定（曲柄摇杆/双曲柄/双摇杆）
  4. 急回特性与行程速比系数K
  5. 压力角与传动角关系
  6. 最小传动角设计准则
- [ ] 每个知识点设计：1道诊断题（车道A路由判断）+ 1-2道变式题（车道C自测复习用，同时可对接【Skill-泛雅作业管理】AI批阅或自建判分逻辑）
- [ ] 每个知识点写**两版**标准讲解话术（对应差异化教学，车道B按掌握度选用）：基础版（更多类比+图示）、进阶版（直接上公式推导）——这是把 [教学智能体方案讨论.md 8.1](教学智能体方案讨论.md) 的"差异化不止调难度，还要调讲法"落到具体产出物
- [ ] **是否/如何导入【问答库】仍是 TBD**——2026-08-21 讨论初步共识：问答库是"命中即返回固定答案"的机制（不经大模型临时生成），适合放高频问题标准定义、易混淆对澄清；不适合放需要苏格拉底式引导、根据学生具体错误动态调整话术的部分（那部分交给系统提示词+任务流）。具体哪些内容进问答库、怎么分类，仍在考虑，先不动手录入
- [ ] **确定教学视频来源**（关键决策点，直接决定车道A要不要做视频跳转分支）：
  - 有：进入知识点-时间戳标注环节，优先标"杆长条件""整转副判断"这两个高频错点
  - 没有：先跳过视频跳转，用图文/问答库讲解替代，后续视频到位后再补【应用库】卡片
- [x] **教材配图提取（2026-09-12完成）**：改用"直接从教材docx提取图"而非AI生成图，避免生成式模型画错机构简图（杆长比例/铰接点位置属于精确性要求高的内容，跟裸模型判断专业对错不可靠是同一类风险）。docx本质是zip包，`word/media/`目录下解压即得。已对`教材/`全部18章+对应图版docx跑了一遍提取，结果：第2/3/4/5/6/7/8/9/10/12/13章共11章有嵌入图片，共66张，按来源文件分文件夹存到 `教材图片/` 下；第1/11/14/15/16/17/18章确认原文件无嵌入图，非提取失败
- [x] **✅ "图片-知识点"映射表已建（2026-09-17）**：见 [教材图片/图片-知识点映射表.md](教材图片/图片-知识点映射表.md)。没有靠人工逐张翻看猜内容，而是写脚本解析原始`教材/*.docx`的`document.xml`，反查每张图的关系ID对应哪个图注（教材图注格式统一是"图X-Y xxx"，比人工看图猜含义更准），再比对`knowledge_base.py`的知识点体系分类。结论：66个文件里只有24张`.png`（来自各章"XX章图.docx"）是真实插图，已全部映射到9个知识点（`mechanism_dof`3张/`lever_condition`等连杆3个知识点各1张已配/`cam_mechanism`1张/`gear_involute`2张/`intermittent_motion`2张/`key_spline_connection`7张/`worm_gear`1张/`belt_chain_friction_drive`5张）；另外42个`.wmf`经排查全部小于2KB，抽查图注上下文确认是行内公式符号的矢量渲染（不是插图），已在映射表里单列说明为什么不建议归入知识点配图体系。抽查了2张PNG（外齿轮各部分名称、圆柱销）用Read工具肉眼核对，与图注完全吻合，方法可信。
- [x] **✅ 24张图已实际接入`media_options`（2026-09-17）**：[knowledge_base.py](测试外部agent服务/knowledge_base.py)给9个知识点（`mechanism_dof`/`cam_mechanism`/`gear_involute`/`intermittent_motion`/`key_spline_connection`/`worm_gear`/`belt_chain_friction_drive`）补了`media_options`，加上原有连杆机构3个知识点，共10个知识点、26条媒体引用。[server.py](测试外部agent服务/server.py)的`TEXTBOOK_IMG_DIR`从只指向"第三章图"单文件夹扩展为指向`教材图片/`根目录，`IMAGE_MANIFEST`从3条扩到24条。验证：脚本核对24个文件全部真实存在、knowledge_base里引用的content_id没有一个是manifest里没有的（防止LLM幻觉id）；起服务器实测`/media/`和`/viewer`两条路由，正常返回图片和渲染页面，未知id正确404。9个知识点的讲解现在能真正配上教材原图，不再是"暂时留空"状态。
- [x] **✅ LLM从Anthropic切换到百度千帆（2026-09-17）**：手头没有Anthropic key，但千帆key已实测可用（`.env`里`QIANFAN_API_KEY`，已加入`.gitignore`）。[llm_client.py](测试外部agent服务/llm_client.py)的`_call_anthropic`改造成`_call_llm`，请求/响应格式从Anthropic Messages API换成千帆v2 chat/completions（OpenAI兼容格式），默认模型`deepseek-v4-flash`（与robot.chaoxing.com任务流意图识别节点用的同一个模型，成本/效果口径统一）。`generate_explanation_and_media()`和`classify_knowledge_points_llm()`两个对外函数签名、降级策略都没变，调用方`mastery_model.py`/`server.py`不用改。实测：知识点打标一次成功识别出没有直接命中关键词的语义转述；讲解生成质量不错，四步结构执行到位，媒体决策判断也对（动态问题正确选了`linkage_3d`）。真实API key就位后，`generate_explanation_and_media`偶发一次JSON解析失败（模型输出里反斜杠转义问题），已有的降级机制正确兜底成MOCK，没有断链路——这是长文本+JSON混排的通病，不特定于某个模型，暂不需要额外处理
- [ ] **TODO：图片实际接入方式待后台实测确定**（阻塞：账号会话过期，需要重新登录）——核心问题是这些图传上去后走哪个入口、能不能真的在对话气泡里显示出来，候选方案见 3.2 新增测试项

## 阶段三：平台功能验证（技术核心，决定阶段四怎么搭）

**工具**：robot.chaoxing.com 后台，登录后逐项在对应菜单下实测

### 3.1 验证数据来源是否真实可用（已实测，见下方结论）

- [x] 【资源管理→Skills→泛雅课程信息】测试调用 → **通过**。真实返回账号下52门课程（含课程名称/课程ID/教师/状态），确认关联课程：
  - 机械原理课程设计（25-26-1）courseId `254588301`，王军老师，11个班约235人
  - 机械设计基础A2（25-26-1）courseId `254589279`，王军老师，6个班约408人
- [x] 【资源管理→Skills→课程学情分析】测试调用（安全等级B中危级）→ **部分通过，关键分支点**：
  - "机械原理课程设计（25-26-1）"：系统显示**"本课暂无知识点"**，无法按知识点分析，只能拿到班级整体数据（任务点完成率/作业分/综合成绩/签到率），且综合成绩普遍很低（10~31分），此课程偏线下实践考核，线上数据本来就少，**不建议用这门课**
  - "机械设计基础A2（25-26-1）"张中利-铸造231-232塑型231班（90人）：**系统已配置33个知识点，其中就有"平面连杆机构"**⭐（还有"零件，构件""自由度计算"等），说明知识点体系已经跟教材章节对齐，**建议以这门课/这个知识点体系为准**
  - ⚠️ 但目前该班33个知识点的**掌握率、完成率全部是0.0%**——不是接口坏了，是这门课还没布置任何关联知识点的测试/作业，没有数据积累。**要让这条链路产出有意义的数据，必须先在学习通后台给课程布置关联"平面连杆机构"等知识点的测试/作业**
  - ⚠️ **技术稳定性问题**：这次查询实测耗时近6分钟，中途多次遇到400错误、SSE流超时，AI被迫写了4个Python调试脚本才拿到数据。说明底层"study-ai skill talk"接口响应慢、不稳定，**如果任务流里要依赖这个Skill做实时诊断反馈，必须做好超时兜底和重试机制，不能假设它秒级返回**
- [x] ~~【资源管理→插件→知识点双率统计】~~ —— 2026-08-23实测证伪：这不是一个真实存在的资源。查了【资源管理→插件】列表（当前项目下完全为空，印证3.5"插件全自助"的结论）和全部29个【资源管理→Skills】（完整列表见3.5下方），均无"知识点双率统计"这个名字，此前计划里这条是凭空猜测/记错了，直接划掉，不再作为待验证项
- [x] 【资源管理→Skills→泛雅作业管理】测试通过 —— 2026-08-23在【预览与调试】里问"查一下机械设计基础A2这门课张中利-铸造231-232塑型231班现在有哪些作业"，Skill 正确定位到课程（courseId 254589279）和班级（clazzId 126556119，注意实际班级名是"塑性231"不是"塑型231"，读音同字不同，AI能自动纠正模糊匹配），分别查了班级作业接口和课程作业接口，**结果都是空**——与3.1"33个知识点掌握率全部0.0%"互相印证，再次确认这门课目前没有布置任何作业/测试，不是接口问题。"AI批阅设置"和"批阅进度查询"两个子能力本次没有触发到（没有作业可批阅），等课程侧布置了作业后再测

### 3.2 验证交互能力

- [x] 【资源管理→应用库】点"添加应用"，用测试链接（YouTube embed + `?start=93`秒参数）建了一张卡片验证自助建卡流程本身没问题——**时间戳跳转的实际效果未验证**：应用库卡片不能直接在对话里触发预览，需要接入任务流的"应用"节点或问答库才能在聊天里弹出，这条留到真正搭车道A时随4.0一起测，不单独验证
- [x] 【资源管理→任务流】新建最小任务流（骨架测试-杆长条件诊断，开始→大模型生成→结束）测试意图识别，**关键发现（比预期更重要）**：
  - **试运行不是"直接对着某个节点测"，而是测整个智能体的意图路由**——第一次直接发学生的"回答"（"应该是双摆杆机构吧..."），平台的顶层意图识别没匹配到这条任务流的触发问题，落到了别的Skill（又触发了一次泛雅作业管理查询），完全没进我搭的流程。只有发送与【开始】节点"标准问题/相似问题"高度相似的话，才会真正路由进这条任务流——**这对阶段四设计是重要提醒：车道A/B/C/D 之间、以及每条车道内部的意图路由精度，强依赖标准问题/相似问题配置得够不够全，不能只指望模型"聪明"**
  - 待续测：把学生的实际错误回答（"完全不会"/"会但算错"/"概念混淆"三种）分别发送验证分类精度——这次只验证了流程本身能跑通，还没系统测三种错误类型的区分度，留到搭建4.1车道A时正式测
- [x] **信号提炼节点合并进意图识别节点——JSON合并输出机制本身可行，但补做泛化测试后发现两个更严重的问题（2026-08-23）**：在大模型生成节点的提示词里同时要求输出 `judge`（四类判断）和 `signal`（signal_type/polarity/evidence）两个JSON字段，一次调用稳定输出两部分内容，格式层面没问题。但只测正向案例（用触发问题原句本身）是不够的，补测了几种更真实的输入后：
  - ⚠️ **【严重】判断准确率不可靠——出现假阴性**：发一个教科书式完全正确的答案（"是曲柄摇杆机构，因为最短杆作为连架杆时，与机架相邻的那根最短杆能整周转动，成为曲柄"——这是标准力学结论），模型把 judge 判成了"概念混淆"，而不是"完全正确"。**这不是边界模糊案例，是明确的机构学知识性错判**，说明单靠 deepseek-v4-flash 裸模型判断专业对错不可靠，4.1车道A搭建时必须想办法让判断有据可依（比如提示词里嵌入标准答案/评分要点，或接知识库做RAG校验），不能假设模型自己"懂"机械原理
  - ⚠️ **【严重】意图路由极其脆弱——真实学生回答可能完全进不了任务流**：同一个测试流程里，发一句非常真实的学生反应"这个我真不知道啊，没学过"，结果是**"当前任务流无法触发"**——完全没进这条任务流，路由失败。前一条消息（教科书答案）能进，这条不能进，说明【开始】节点靠"标准问题/相似问题"做语义匹配的路由机制，对"学生用自己的话回答"这种真实场景覆盖率很不稳定，不能只加两三条相似问法就当作解决了。**这对整个四条车道的架构是个警讯**：如果学生的常见反应模式（"不知道"、"忘了"、"这个没听过"等）触发不了对应车道，学生体验会直接崩掉
  - **结论调整**：判断+信号合并输出这个JSON机制本身没问题，可以保留（省一次调用），但4.1正式搭车道A时，(a) 【开始】节点的相似问题必须覆盖"完全不会/答非所问/态度类回应"这类非知识性表达，不能只覆盖"像是在回答"的句子；(b) judge判断环节需要补充标准答案/评分依据，不能让模型裸判专业对错；这两点都要在4.0/4.1联调时专门测，不能想当然
- [x] ~~【基础设置→触发器设置】"事件触发"枚举值~~ —— 2026-08-23实测证伪：点开"添加触发器"选"事件触发"后，"模式"只有唯一固定选项【Webhook(Catch hook)】，平台生成一个专属URL+Bearer Token，逻辑是**外部系统调这个URL来通知平台**，不是"平台内部监听学生行为自动触发"。也就是说它不是一个可选的行为事件枚举列表，而是通用的入站webhook接收器。**结论：不能指望用"事件触发"实现"同一知识点反复被问≥2次→自动推送视频"这类平台原生行为检测，这个逻辑必须写在任务流内部（用计数变量+条件判断节点），依赖清单/技术不确定性表里原来就写的"备选方案"现在确认为唯一可行方案，不是备选，是唯一解
- [x] ~~【基础设置→访客端设置→访客登录】"第三方认证地址"说明文档~~ —— 已在3.5实测记录，是重定向式SSO，需要学校IT提供地址，不是我们能自己配置的，结论同3.5，不重复验证
- [x] **教材配图能否在对话里实际显示（2026-09-15实测，两种方式都失败，比预期更棘手）**——已从教材docx提取出66张标准机构简图（见阶段二），实测了两条候选路径：
  1. ❌ **【结束】节点输出内容里塞markdown图片语法**（`![说明](图片URL)`）——试运行结果：聊天气泡里原样显示这段markdown源码文本，只把URL部分转成可点击超链接，图片本身完全没有内嵌渲染出来
  2. ❌ **【资源管理→应用库】建卡片（标题+封面图+PC链接），接入任务流【应用】节点**——用`file_upload`直接把提取的机构简图上传成卡片封面，确认卡片本身在应用库里建成功、图片清晰；接入任务流【开始→应用→结束】（连线由用户手动完成，画布拖拽这类精细操作已写入CLAUDE.md规则：尝试2-3次不行就交给用户）后试运行，**这张自建卡片在聊天里完全没有显示**——只有【应用】节点列表里的两个系统内置应用（留言板/人工服务）会显示成小图标+文字的按钮，自建应用（无论是否勾选启用）都没有出现在回复里，用`get_page_text`确认过配置本身是存在的，不是没保存成功
  **结论**：目前测过的两条路径都走不通，"图文并茂"这件事在当前任务流机制下比预期难。剩下唯一没测的候选是【资源管理→知识库】图文混排召回（知识库检索召回文档片段时会不会把嵌入的图一起带出来），需要在4.1之前专门测这条，如果也不行，可能要考虑退而求其次——不在对话里直接显示图，而是让讲解文本描述图的内容+给一个可点击的"查看配图"外部链接（跳到我们自己托管的图片页面）

### 3.3 验证结果决定的分支

- 如果 3.1 里的官方 Skill/插件能拿到真实、细颗粒度的数据 → 表B(掌握度画像)可部分由官方数据回填，减少自建统计逻辑
- ~~如果拿不到（比如课程还没建、数据为空、颗粒度太粗） → 完全依赖【资源管理→数据源→添加表单】自建的表A/表B~~ —— **此路线已被 3.4 实测证伪，见下**

### 3.4 骨架搭建实测（2026-08-23，重要发现，直接推翻原表A/B实现路径假设）

> 背景：登录 robot.chaoxing.com 后台实际动手搭 4.0 公共基础设施骨架，过程中发现平台"数据源/数据库"能力和申报文档最初假设的不一样。

- [x] **【资源管理→数据源→添加表单】不能自建表** —— 点开弹窗提示"以下数据来源于单位微服务表单"，只能从机构管理员（张丽敏）已创建的表单列表里选一个绑定，列表里全是无关的行政表单（专家评审列表/院系门户/教材管理/教师团队/调研报告管理/培养方案建设），**没有"新建自定义字段表单"的入口**。原计划"自建学生答题记录表，字段包含学生ID/知识点/信号类型..."这条路线在这个入口走不通。
- [x] **任务流【表单】类节点（新增数据/删除数据/查询数据/更新数据/表单检索）同样受限** —— 这些CRUD节点的"表单"选择器指向的就是上面同一份机构级列表，不是项目私有的自建表。
- [x] **任务流【数据库→查询数据】节点也是同源限制** —— 支持SQL自定义查询/字段查询，但"选择数据库"弹窗同样是从机构预置列表选（当前0条），不是自助建库。
- [x] **发现潜在出路：任务流【功能节点→代码】支持写Python** —— 允许用任务流变量做输入参数，`return`语句输出结果。理论上可以在代码里发HTTP请求，调用一个自建的外部轻量后端（如免费serverless函数+真实数据库），从而绕开机构数据源的权限限制。**未实测代码节点本身**（画布拖拽连线操作，尝试几次未成功，已改用【插件】路线验证，见下条，代码节点留待后续需要更灵活逻辑时再测）。
- [x] **✅【插件】出网能力已实测确认可行，端到端成功（2026-09-15~16）**：
  - 之前后台已有一个"测试外部agent"插件（POST到一个trycloudflare.com临时地址），试运行时报错 `UnknownHostException`——分析后判断这是隧道失效导致DNS解析不到，不是平台限制出网（如果平台真的封杀插件出网，报错应该是"连接被拒绝/网络策略不允许"而不是域名解析失败）
  - 为验证判断，本地重新搭建：Python轻量HTTP服务（`测试外部agent服务/server.py`，纯标准库无依赖，监听8899端口，`POST /agent` 收 `{"message":...}` 返回 `{"reply":...}`）+ cloudflared隧道（`测试外部agent服务/bin/cloudflared`，从GitHub releases下载的预编译二进制，没走brew是因为brew要从源码编译go语言环境耗时太长）暴露到公网
  - 把插件地址改成新隧道地址后试运行，**插件节点运行成功（耗时1.25s），聊天里收到本地服务器的真实回显内容**——证实链路完全打通：学生消息→平台路由进任务流→插件节点真实调用外部公网服务→外部服务处理返回→平台正确解析`reply`字段→展示给学生
  - **结论：机构数据源受限这条路线被绕开了，插件是官方支持、已验证可行的外部数据接入方式**，4.0搭建表A/表B的写入信号/读取画像接口，直接走插件路线即可，不必再纠结代码节点
  - 副产品发现：测试过程中用不精确的话术"你到底是谁"（相似问题只配了"你是谁"）导致"当前任务流无法触发"，再次印证3.2节记录的路由脆弱性问题，同一批测试里换回精确触发词才成功——这个问题依然没解决，跟插件出网是两回事，4.1前仍需专门处理
- [x] **✅ 回复的视觉呈现方式（图片/3D等）能否由我们自己的agent决定——已实测端到端成功（2026-09-16）**：
  - 背景：确认插件出网可行后，进一步验证的问题是——走到自建agent之后，除了文字回复，"加载特定图片/3D动画"这类视觉呈现能不能也由我们自己的判断逻辑决定，而不是被平台的固定回复格式限制住
  - 搭建路径：本地`server.py`扩展出`CONTENT_MAP`（按知识点关键词映射标题/颜色/说明），`POST /agent`除了`reply`文本外新增返回`content_id`字段（服务端根据消息里是否含"曲柄"/"摇杆"/"双曲柄"等关键词判断），另加`GET /viewer?content=<id>`路由返回一个按`content_id`渲染不同样式的演示页面
  - 任务流搭建：开始→插件（调用外部agent，输出`reply`+`content_id`）→**嵌入**节点（PC端iframe地址填`https://.../viewer?content={{content_id}}`，用`{{变量名}}`语法在iframe地址框里直接触发动态拼接）→结束（输出`{{reply}}`文本）
  - 中途踩坑：① 嵌入节点"输入"区块的变量值绑定下拉一度显示"暂无数据"，后来重新打开就能看到插件节点的`content_id`可选并绑定成功——**不是不可用，是这个下拉需要上游节点先真正连线+跑过至少一次后才会刷新出选项**；② 试运行报错"嵌入-引用变量值不可为空"，原因是"输入"区块只填了变量名`content_id`、没选变量值来源，补上"插件-content_id"绑定后解决；③ 插件→嵌入、嵌入→结束这两条画布连线拖拽多次失败，按[[feedback_handoff_finicky_browser_ops]]规则交给用户手动连接
  - **验证结果**：试运行输入"曲柄摇杆机构怎么判断"，插件节点正确判断`content_id: "crank_rocker"`，嵌入节点正确插值生成`pcIframeUrl: https://dat-desired-presence-wells.trycloudflare.com/viewer?content=crank_rocker&...`，结束节点正常输出`[本地测试后端已收到] 你说的是：曲柄摇杆机构怎么判断`，全链路耗时约2.4s
  - **结论：视觉内容展示不是只能是固定卡片/纯文本，可以由外部agent的判断逻辑（`content_id`）动态决定嵌入哪个页面/图/动画**，"套壳"架构的最后一块能力拼图（回复文字+回复视觉都能自主决定）已验证打通，4.1正式搭建时可以直接复用这套"插件出content_id → 嵌入按content_id渲染"的模式，把content_id换成真实知识点/机构类型/学生所处车道等业务判断结果即可
- [x] **✅ 进一步验证：真实3D动画（而非纯色демo页）同样能被同一套机制加载（2026-09-16）**：
  - 把 [四连杆3D模拟器](四连杆3D模拟器/index.html)（Three.js实时交互模拟器）接入同一条链路：`server.py`扩展出`/3d/`静态文件路由（直接托管`四连杆3D模拟器/`目录下的`index.html`+`main.js`），`/viewer?content=linkage_3d`收到这个content_id时302重定向到`/3d/index.html`；`POST /agent`的判断逻辑新增：消息含"3d"/"动画"/"模拟"关键词时输出`content_id: "linkage_3d"`
  - 用消息"曲柄摇杆机构的3d动画演示一下"（同时含路由关键词"曲柄摇杆"+触发词"3d"/"动画"）试运行，`插件`节点正确输出`content_id: "linkage_3d"`（本地`curl`直接验证过判断逻辑），`嵌入`节点据此插值出`.../viewer?content=linkage_3d`
  - 直接打开这个公网iframe地址（`https://dat-desired-presence-wells.trycloudflare.com/viewer?content=linkage_3d`）验证：302正确跳转到`/3d/index.html`，页面正常加载出Three.js渲染的三维四连杆机构，滑块交互、Grashof机构类型判定、动画运转均正常，两次间隔截图对比确认曲柄杆角度在实时变化（不是静态图）
  - **结论：不仅是简单的静态展示页，连需要外部CDN依赖（unpkg.com加载three.js）、JS运行时、canvas实时渲染的完整交互式3D应用，都能通过"插件判断content_id→嵌入节点动态iframe"这套机制正常加载运行**，视觉呈现的复杂度上限不是问题，4.1可以直接把这个3D模拟器或类似的交互式教具接入到真实的知识点讲解车道里
  - 踩坑记录：验证过程中【插件】节点的"展开运行结果"链接被误点了3次，其中3次都触发了节点被误删（点击区域和删除图标距离太近，用44%~47%画布缩放时坐标精度不够）。按[[feedback_handoff_finicky_browser_ops]]规则，这类"展开运行结果"/编辑图标一类的精细UI操作，之后应优先请用户代为点击，或提高画布缩放比例（85%+）后再尝试，不要在低缩放比例下反复自己点
- [x] **✅ 外部agent后端实现"知识点关键词全覆盖路由 + 讲解生成 + 媒体决策"完整逻辑代码（2026-09-16）**：
  - 背景：确认前面测的都是硬编码demo后，落实"教材里所有知识点关键词都路由到我们自己的agent"这句话——把本次大赛实际范围（机械原理《平面连杆机构》一章，6个知识点：杆长条件与机构类型判定、整转副存在条件、死点位置及应用、急回特性与行程速比系数K、压力角与传动角关系、最小传动角设计准则）的关键词全部纳入分类路由，而不是只测过的"曲柄摇杆"几个词
  - 新增 [知识库模块](测试外部agent服务/knowledge_base.py)：6个知识点的关键词列表+标准内容+可用媒体清单（`media_options`），`classify_knowledge_point()`按关键词命中数给消息打分分类，命中最多的知识点胜出，完全不命中返回None（对应"不在这几个知识点范围内"的兜底回复）
  - 新增 [LLM调用模块](测试外部agent服务/llm_client.py)：把提示词库第7条模板程序化，`generate_explanation_and_media()`拼好prompt（知识点+标准内容+学情状态+媒体清单+学生原话）调用大模型，返回`{explanation, media:{content_id, reason}}`。**真实API key还没有，先用`ANTHROPIC_API_KEY`环境变量占位**——没配key时自动降级成规则版MOCK（关键词近似模拟"动态过程→选3D""静态结构→选配图""已掌握+低详略→选none"的判断逻辑，输出统一带`[MOCK-LLM]`前缀标注，不会被误当成真实模型输出），真实调用失败时也会降级到MOCK而不是直接报错断链路
  - 改造 [server.py](测试外部agent服务/server.py)：`/agent`接口改为"分类→生成"两步；新增`/media/<content_id>`静态路由，把`教材图片/第三章图/`下已提取的3张教材原图（结构简图、急回特性示意图、压力角传动角示意图）接进`/viewer`渲染，不再是之前测试用的纯色demo块；`/viewer`按content_id分三种情况处理：`linkage_3d`→302到3D模拟器，教材图片id→渲染带原图的页面，`none`/未识别→"本轮无需配图"兜底页
  - 本地测试6个case全部符合预期：结构类问题（"曲柄摇杆怎么判断，长什么样"）→配教材结构图；动态过程类问题（"摇杆的运动过程是怎样的"）→选3D；已掌握+低详略度的确认类问题→选none；无关问题（"食堂吃什么"）→正确拒绝并给出"不在知识点范围"的兜底话术；`/media/`和`/viewer`两个路由本地和公网隧道都验证了200/302正常返回
  - **待办**：等真实LLM API key到位后，把`llm_client.py`里的`ANTHROPIC_API_KEY`环境变量配上（模型/BASE_URL可通过`ANTHROPIC_MODEL`/`ANTHROPIC_BASE_URL`环境变量调整），不需要改任何调用方代码；另外目前只有"杆长条件"知识点配了3D模型，其余5个知识点暂时只有静态图或完全没有专属素材（复用3D模型或none），后续如果要给其他知识点也配3D/图，在`knowledge_base.py`的`media_options`里加`MediaOption`条目即可
- [x] **✅ knowledge_base.py从6个知识点扩展到覆盖全书18章共26个知识点（2026-09-16）**：起因是导入了一份1220题的题库实测覆盖全课程（不止连杆机构一章），`standard_content`全部摘自已网络检索交叉验证的[知识图谱.md](知识图谱.md)，不是现编的；同时用同一套`classify_knowledge_point()`给题库1220题打知识点标签（985题/80.7%成功归类，235题待人工归类，见[知识点聚类预览.md](题库/知识点聚类预览.md)）；另外按18章教材原文手写生成了231道AI辅助题库（`source="ai-gen"`，见[AI生成题库.json](题库/AI生成题库.json)），每题都能在对应章节教材原文里找到依据，schema校验全部通过
- [x] **✅ 表A「学习信号记录表」补全 + 用Dialogue-KT论文思路升级知识点打标（2026-09-16）**：
  - 背景：发现`mastery_model.py`（表B，CDM Q矩阵+Elo+遗忘曲线，另一会话/协作者加入）之前`record_answer()`直接从"对/错"跳到Elo评分，跳过了[教学智能体方案讨论.md 9.5](教学智能体方案讨论.md)设计的表A这一层，也没法接住自报/自我解释/可视化探索这几种非做题信号
  - 新增 [signal_log.py](测试外部agent服务/signal_log.py)：表A的完整实现，字段对齐9.5原文（学生ID/知识点ID/信号类型/极性/证据摘录/时间戳），JSONL追加写；实现了"反复问同一知识点-1"的自动判定（24小时内第3次提问同一知识点，极性自动强制覆盖成"负"，不管调用方原本传什么）
  - 改造`mastery_model.py`：`record_answer()`现在先写表A再更新表B；新增`record_qualitative_signal()`处理非做题信号，用固定步长（±15分，对应Elo同量纲）实现9.5"自报已懂+1、自我解释不出依据-1"的简化打分规则
  - `server.py`新增`POST /signal`（车道B/D调用，写非做题信号）、`GET /signals`（读表A原始记录）
  - 本地测试6种场景全部符合预期：做题答对→自报已懂(+15)→自我解释说不出依据(-15)→连续问3次同一知识点(第3次自动判负-15)，表A能完整读回原始记录，表B正确聚合成最终rating；做题信号误用`/signal`接口被正确拒绝(400)
  - **顺带读了[参考论文/Dialogue-KT_Knowledge_Tracing_in_Dialogues.pdf](参考论文/Dialogue-KT_Knowledge_Tracing_in_Dialogues.pdf)**（LAK25会议论文，UMass/UPenn），评估其"LLMKT微调模型做知识追踪"的核心方法不适合我们（需要训练数据+GPU微调基础设施，论文自己的结果也显示小数据量下传统KT方法≈瞎猜，是个值得注意的现实校准，不该指望`mastery_state`在真实小样本下有多准）；但借鉴了它"用LLM给每轮对话打知识点标签、不训练只是prompt、人类专家评估准确率0.93/1"这个具体做法——在[llm_client.py](测试外部agent服务/llm_client.py)新增`classify_knowledge_points_llm()`，配了真实API key时走LLM语义分类（能理解"最短杆和最长杆之和"这种没有直接命中关键词的转述），没配key或调用失败时自动降级成关键词匹配（`knowledge_base.classify_knowledge_points_multi()`），接口形状不变；`mastery_model.py`的`get_q_matrix()`已切换到这个新函数
- [x] **✅ 题库配图展示机制已实现并验证（2026-09-16）**：题库导入时提取出161张本地图+若干超星/网易云外链图，复用"插件出content_id→嵌入渲染iframe"这套已验证架构——新增`content_id`格式`quiz_<question_id>_<image_index>`（如`quiz_q_0040_0`），`server.py`新增`/quiz-media/<filename>`静态路由托管本地题图，`/viewer`遇到`quiz_`前缀时按图片来源分流：本地图渲染包装页，外链图直接302重定向（不需要代理，超星/网易云CDN本身就是公网可访问的）。过程中发现并修复一个真bug：外链图片URL里的`&`在原始HTML里是`&amp;`实体，提取时没做反转义，导致重定向地址错误（如`&amp;quality=100`不会被当成独立参数）——已在`parse_question_bank.py`的`extract_images()`里加上反转义，重新生成了`题库.json`。本地+新的公网隧道（`penguin-brown-shake-moves.trycloudflare.com`，前一个隧道已断线）都验证了本地图和外链图两种类型都能正确渲染，浏览器截图确认图像清晰可辨
- [x] **✅ 按教材18章内容，在[3d动画/](3d动画/)下新增11个章节的3D运动机构模拟器（2026-09-17）**：
  - 背景：已有的[四连杆3D模拟器](四连杆3D模拟器/)只覆盖第三章，用户要求给教材其他章节也做类似的3D动画。先评估了18章内容性质——不是每章都适合做"运动机构"动画（第1/2/8/9/11/14章本质是抽象概念/强度理论，硬做3D动画会误导学生以为是个机构），跟用户确认后定了范围：9个真运动机构（第4/5/6/7/12/13/15/17/18章）+2个静态3D装配展示（第10/16章），共11个
  - 新增 [3d动画/common.js](3d动画/common.js) 公共模块：场景/相机/光照/网格/OrbitControls的样板代码，以及一个"示意性"齿轮外形生成函数（梯形齿近似渐开线，不是真实渐开线曲线，但齿数/模数/分度圆半径/传动比等运动学参数精确计算——跟四连杆模拟器"杆件用圆柱体示意、不做真实截面"是同一条简化原则，教材原图仍是`教材图片/`里提取的真实图，3D模型只做交互动画，不替代精确机构图）
  - 各章节运动学设计要点（均可推导验证，非拍脑袋）：
    - **第4章凸轮机构**：推程/远休止/回程/近休止四段运动规律（推程回程为简谐运动），从动件位移s(θ)=基圆半径外的凸轮轮廓变化量，与从动件形式（滚子/尖顶）无关——这是教材強调的"运动规律设计"和"从动件结构选型"两个独立问题
    - **第5章齿轮传动**：w1·z1=w2·z2啮合关系，中心距a=m(z1+z2)/2，外啮合反向，公式精确计算
    - **第6章轮系**：单排行星轮系（内齿圈固定），同心条件Zr=Zs+2Zp，传动比i=(Zs+Zr)/Zs，行星轮自转/公转分别推导（自转=系杆角速度+相对系杆的啮合自转），3个行星轮均布
    - **第7章间歇运动机构**：槽轮机构用精确向量几何逐帧计算（销相对槽轮中心的方位角=槽轮瞬时转角，这是槽轮运动学的核心关系），驱动转角=180°-360°/n（与教材n=4时"90°驱动+270°停歇"吻合）；棘轮机构用摆角=半齿距的正弦往复驱动，每次正行程精确前进一个齿。调试中发现并修了一个"啮合窗口从中点开始导致首次只转半程"的启动瞬态小问题
    - **第12章蜗杆传动**：多头蜗杆用多条独立螺旋线管状体表示头数z1，传动比i=z2/z1，两轴交错90°
    - **第13章带传动/链传动**：核心是外公切线几何（两圆半径r1,r2、圆心距d，切点角度=90°±asin((r1-r2)/d)），皮带/链条路径=两段切线+两段圆弧，调试中发现并修了一个"填充式Shape会把两轮间整块区域填满"的错误（改成沿中心线路径的细管/链节表示）；带传动额外加了打滑率滑块示意摩擦型传动的弹性滑动
    - **第15章滚动轴承**：深沟球轴承，保持架角速度wc=wi·ri/(ri+ro)，滚动体自转按纯滚动无滑动约束推导（同行星轮系方法论）
    - **第17章离合器**：牙嵌式接合瞬间刚性同步（无过渡）、分离后摩擦衰减到0；摩擦式接合/分离都是指数渐进过程，示意"打滑"这个关键特征差异
    - **第18章弹簧**：螺旋线管状体随高度压缩变化（圈数不变、高度线性变化的理想线性弹簧简化），载荷板往复运动模拟Fmin~Fmax工作循环
    - **第10章（静态）**：螺栓螺母连接/键连接/销连接，三个tab+装配-分解滑块，无运动但可旋转查看结构
    - **第16章（静态）**：滑动轴承轴颈偏心示意，偏心率ε=e/c可调，直观展示"重载/低速时轴颈贴向轴承壁、油膜濒临破裂"这个核心概念
  - 全部11个逐一在浏览器里实际跑过验证（截图确认渲染正确、控制台无报错），不是写完就交差；调试中揪出并修复了两个真实的实现bug（槽轮启动瞬态、皮带填充错误），不是想当然
  - launch.json新增`3d-anim`预览配置（`python3 -m http.server 8732 --directory 3d动画`）
- [x] 发现平台"记忆"标签页下有个**内置"长期记忆"开关**，tooltip说明是"通过用户发送的内容，提取用户画像，用于更好地响应用户的提问"——黑盒式，字段/结构不受我们控制，**不能替代表A/B的结构化需求**，但可以作为辅助手段先开着当兜底个性化，不冲突。**2026-09-17已实际打开**（之前一直是关闭状态）：连带打开了子开关"支持在智能体问答过程中参考用户的长期记忆"（不开这个的话，长期记忆只采集不生效，白开）、记忆有效期保持"永久有效"。同时发现【记忆→变量】里列出的系统变量含`INNER_userId`——正是9.5设计里表A/B用的持久学生ID，确认平台原生提供这个稳定标识，不需要自己另外造一套用户ID方案
- [x] **✅ 系统提示词新增"新老学生个性化"小节 + 提示词库.md 全面同步成全书范围（2026-09-17）**：起因是这次核实发现[提示词库.md](提示词库.md)里第1条(全局系统提示词)和第7条还停留在"只做平面连杆机构""6个知识点"的旧草稿状态，跟已经上线、覆盖全书18章的真实系统提示词、已扩到26个知识点的`knowledge_base.py`不一致，借这次一起同步。同时补上"学生二次登录、已有表A/B数据时讲解风格要相应改变"这个需求：系统提示词里新增一段"个性化：区分新学生与老学生"（新学生简单介绍功能不假设已知情况；老学生直接接上次卡住的地方，不从零讲起；收到学情摘要变量时按掌握度/详略偏好调整讲解深度和语气），从1980字增到2460字（5000上限内）；[提示词库.md](提示词库.md)第3条"共享个性化层节点"提示词同步加了`is_returning_student`/`welcome_style`字段，判断依据是插件查表B时"完全没有记录=新学生，有至少一条真实信号=老学生"，不需要另外猜测逻辑。**现状说明**：这段文字现在依赖的是对话历史+刚打开的平台长期记忆功能这类"软性"个性化，真正结构化、可靠的个性化要等共享个性化层节点真正接入表B插件后才算完整落地，这是渐进关系不是互斥关系
- [x] 小坑：任务流后台左侧菜单每次切换都会弹"请确定是否离开当前页面"确认框，不代表真的有未保存改动，是平台UI小毛病，点确认即可继续，不影响数据。
- [x] 已确认系统提示词此前已写了一部分雏形（角色/目标/技能1/技能2，2026-08-21成果仍保留在后台），阶段四不是从零开始。

**由此产生的新分支决策（2026-08-23 补充探路后更新优先级）**：
- [x] **首选路线：【资源管理→插件→添加插件】自建HTTP API接口——✅ 2026-09-15~16实测端到端成功**，不再是"理论可行"而是已验证：自建插件POST到自己电脑上跑的Python服务（经cloudflared隧道暴露到公网），试运行插件节点运行成功、拿到真实回显内容，完整链路打通。做法确认：自建轻量外部后端（数据库+简单API，如 POST /signal 写表A、GET /profile 读表B），注册成插件，任务流【功能节点→插件】节点直接调用——比等机构管理员建表更可控，也比"代码节点发HTTP请求"更贴合平台设计意图。**遗留问题**：这次用的是免费临时隧道（trycloudflare.com quick tunnel），进程关闭/重启地址就变，正式搭建4.0时需要换成稳定托管（如Railway/Render部署+固定域名），不能长期依赖本机+临时隧道
- [ ] **备选路线A**：找机构管理员（张丽敏或其他有权限的人）确认能否新建一个自定义"微服务表单"给机械小助手项目用，字段按表A/表B设计（见 [教学智能体方案讨论.md 9.5](教学智能体方案讨论.md)）——首选路线已验证可行，这条备选优先级进一步降低，非必须
- [ ] ~~备选路线B：验证【代码】节点的网络出网权限~~ —— 首选的插件路线已经端到端跑通，代码节点不再是必须验证项，除非后续需要在同一步里做更复杂的数据清洗/打分逻辑，再按需验证
- [ ] 三条路线都不通的兜底方案：退化为**纯会话内变量**——已不需要，首选路线已验证可行

### 3.5 平台其他模块探路（2026-08-23，回答"任务流/后台各模块能做什么"）

> 背景：搭骨架之余顺带把后台其他还没细看的模块过了一遍，明确各自的能力边界和是否受机构权限限制，供阶段四/五设计时直接引用。

- [x] **【资源管理→应用库】完全自助**：填标题/封面/PC端链接/移动端链接即可生成一张卡片，不受机构权限限制——车道A"反复答错推送视频卡片"、教师端课堂演示都可以用这个
- [x] **【资源管理→问答库】自助的问答对列表**（问答内容/来源/命中总次数/创建人），目前是空的，随时可以开始录入
- [x] **【资源管理→智能学习】学生反馈看板**：不满意问题/满意问题/未知问题/人工回复问题/大模型兜底回复问题五个标签页——对应阶段五"筛选不满意回答反哺问答库/知识库"这一步，功能确认存在
- [x] **【资源管理→MCP服务】六个预置工具**（网页解析/联网搜索/图片OCR/天气预报/办公写作/教育教学）+ 支持"手动创建/从JSON导入"自定义MCP服务——比插件更重，适合未来做更复杂的多工具编排，MVP阶段用插件就够
- [x] **【基础设置→触发器设置】只有"时间触发/事件触发"两类**，时间触发支持"周期触发/定时触发"两种模式+可选时区，触发后执行绑定的任务流——确认教师端"定时推送学情画像"这条设计技术上可行，之前只是理论推测，现已在UI里验证到具体配置项
- [x] **【基础设置→访客端设置→访客登录】"第三方认证地址"是重定向式SSO**：tooltip原文"填写对应的统一认证地址即可，'refer='后系统会自动拼接编码之后的当前页面地址"——需要学校IT部门提供统一身份认证的入口地址（类似CAS单点登录），不是我们项目组自己能配置出来的，需要单独找学校信息化部门要这个地址，列入依赖清单
- [x] **新发现：两个"推送通知"类Skill，直接解决阶段五"学情画像自动推送"的最后一环**（2026-08-23浏览全部29个内置Skill时发现，此前完全没注意到）：
  - **泛雅发送通知**：向泛雅教学平台（超星学习通）的课程班级发送通知，以及查询课程下的通知列表——适合"给整个班级推送一条学情简报"
  - **notice-push**：推送通知消息到超星系统，支持通过手机号或UID指定收件人——适合"只推给任课老师本人"，比泛雅发送通知更精准
  - 此前阶段五5.2设计的"定时任务生成班级画像"只想到了"生成"，没想清楚"生成完怎么真正推到老师手机/账号上"，这两个Skill补上了这个缺口——触发器（时间触发）执行任务流→任务流里跑完数据分析→末尾接一个调用 notice-push 的节点，直接把结果推给张老师，不需要老师主动登录后台看
- [x] **知识点双率统计不存在**：翻遍【资源管理→插件】（当前项目下为空）和全部29个【资源管理→Skills】，均无此项，此前3.1里"下一步测这个"的计划是基于错误记忆，已在3.1划掉

## 阶段四：MVP 搭建——按四条车道分别搭，共享个性化层与记忆写回

**前置条件**：阶段二内容已就绪 + 阶段三验证结论已明确

### 4.0 公共基础设施（四条车道都依赖，最先搭）

- [x] **✅ 【基础调试→系统提示词】已重写完成（2026-09-17登录后台核实，比计划描述的还完整）**：现有内容（1980/5000字符）已经是"角色+核心原则+四种场景识别（对应四条车道A/B/C/D）+苏格拉底式引导四条具体规则+学生知识状态记录/查看+输出格式"的完整结构，规则细化程度超过原计划要求。**唯一的清理动作**：删掉了一段硬编码"第三章六个核心知识点"的旧内容（早期只做第三章时写的，跟现在"服务范围覆盖全书18章"的角色设定矛盾），删除后从2320字减到1980字，已保存并二次刷新页面确认服务端持久化成功。**踩坑记录**：第一次用JS直接改`textarea.value`+`dispatchEvent`删除该段后，紧接着点击右侧技能开关，导致Vue的响应式状态和DOM产生冲突，文本被错误拼接（长度对但内容顺序乱了）——所幸还没点保存就发现，靠"刷新页面丢弃未保存改动→重新编辑→编辑后不做任何其他操作→立刻保存→再刷新校验"这个更保守的流程绕开了，以后类似场景要避免"改完一处不保存就去动别的控件"
- [x] **✅ 【基础调试→设置→技能设置】已开启（2026-09-17）**："问答库"、"扩展问题库"（问题联想的两个子开关）、"知识库资源推荐"三个开关已从关闭改为开启并保存确认；"文献传递回答"之前就是开的，保留不动；其余（微应用推荐/用户问题推荐/资源应用推荐/回复翻译）保持关闭
- [x] **✅ 【资源管理→知识库→新增知识库】早就做完了（2026-08-21，本次登录后台核实现状）**：默认知识库下已有26个文件、总计33.3k字符，全部"已完成"状态——正好是18章教材docx + 8个"图"版docx（第二/三/四/五/七/十/十二/十三章各有对应图版），逐页翻查确认第一章到第十八章全部在列，覆盖全书，不需要重新上传
- [ ] 【资源管理→任务流→添加任务流】搭建**意图路由节点**：大模型生成节点，判断学生输入属于车道A/B/C/D中的哪一类，作为整个任务流的第一个分支点
- [ ] 【资源管理→任务流】搭建**共享个性化层节点**：紧接在意图路由之后，四条车道共用，读取表B（学生掌握度画像表）当前状态，输出"内容深度/讲法/优先出题的薄弱知识点"供下游车道使用
- [ ] ⚠️ **建"学习信号记录表"（表A）：学生ID（`INNER_userId`）、知识点ID、信号类型（自报/提问/自我解释/可视化探索/做题）、极性（正/负/中）、证据摘录、时间戳** —— 按 3.4 更新后的首选路线，走【资源管理→插件】自建一个"写入信号"的HTTP API插件（POST /signal），**不能直接照原计划点【资源管理→数据源→添加表单】自建**，那个入口只能绑定机构已有表单
- [ ] ⚠️ 建"学生掌握度画像表"（表B）：学生ID、知识点ID、掌握状态（未学/学习中/薄弱/已掌握）、累计信号分、最近更新时间、**新增：`detail_level`（详略偏好，默认"中"）、`encourage_level`（鼓励/严格语气偏好，默认"中"）** —— 同表A，走【资源管理→插件】自建"读取画像"的HTTP API插件（GET /profile），实现路径细节待 3.4 分支决策落定
- [ ] **新增：教学风格个性化——方案1"学生显式调整"先做（2026-09-12讨论确定）**：不做"模型推断学习风格"（学习科学证据薄弱+裸模型判断已实测不可靠，风险太高，暂不做），改成让学生自己说"讲详细点/简单点就行/别这么啰嗦"这类话来调整`detail_level`/`encourage_level`：
  - 搭一个**轻量意图识别节点**（大模型生成，独立于4.1-4.4里判断对错的高风险judge节点），只做"这句话是不是在要求调整讲解详略/语气"这一个二元/三元分类，风险等级远低于判断专业对错，不易踩到3.2里发现的假阴性问题
  - 识别到调整意图后，用条件判断节点更新表B对应学生的`detail_level`/`encourage_level`字段，写回频率同表A/B已有的写回机制（走插件POST）
  - 默认值兜底：学生没主动表达偏好时用`detail_level=中`/`encourage_level=中`，可按班级整体情况在后台手动调整这个默认值（教师/项目组设置，不依赖模型判断）
  - 共享个性化层节点（本节上方那条）读取这两个字段后，作为变量插进四条车道判断节点的提示词模板（如"detail_level=低时不超过3句话，不展开推导；=高时允许展开公式推导"），跟难度分层是同一套变量注入机制，不引入新的不确定性
  - 方案2（按车道固定语气基调）、方案3（更细的连续参数/规则化微调）留作后续迭代，不在这版MVP先做
- [ ] ~~搭建独立的"信号提炼节点"~~ —— 2026-08-23实测确认可以合并进各车道自己的判断/回复节点（同一次大模型调用输出judge+signal两个JSON字段即可，见3.2），**不必单独搭一个节点**，只需在4.1-4.4每条车道的判断节点提示词里都补上"同时输出signal字段"的要求即可。⚠️ 但同一轮泛化测试也测出judge判断本身不可靠（假阴性）+ 路由覆盖率不足两个更严重的问题，见3.2，4.1正式搭车道A时要专门处理，不能照抄这次的最小测试配置。仍需搭：**表B更新规则**（条件判断节点，简化打分：自报已懂+1、自我解释不出依据-1、做题对+0.5/错-0.5、反复问同一知识点-1，累计滚动分超阈值切换状态）——挂在四条车道的公共出口处，具体信号类型与打分表见 [教学智能体方案讨论.md 9.5](教学智能体方案讨论.md)
- [x] **表B更新规则 v2：引入认知诊断（CDM/Q矩阵）+ Elo式难度加权，2026-09-16讨论确定路线并在外部服务里实现（DKT/IRT/CDM三个方向里的可行性排序，先落地CDM+IRT两个）**：
  - **CDM（Q矩阵，已实现）**：现在的简化打分默认"一道题只更新一个知识点"，但[题库](题库/)里不少题实际同时考察多个知识点（如"整转副判断"依赖先修知识点"运动副与自由度"）。没有走"人工重新标注1220题"这条路，改成复用题库分类已有的关键词打分基础设施——新增 [knowledge_base.py:classify_knowledge_points_multi()](测试外部agent服务/knowledge_base.py) 多标签版本（命中分达到最高分一半以上的知识点都算，而不是只取分最高的一个），[mastery_model.py:get_q_matrix()](测试外部agent服务/mastery_model.py) 按题目ID缓存这份Q矩阵。先修关系扩散更新（薄弱知识点顺带标记先修知识点"需要复查"）还没做，留作后续
  - **IRT（简化成Elo式在线更新，已实现）**：没有做真正IRT要求的联合MLE/EM估计，改用**Elo评分机制**（棋类积分同款思路，Duolingo难度自适应部分也用）：每个(学生,知识点)维护"能力分"、每道题维护"难度分"（初始值从题库.json已有的"难易度"易/中/难字段映射，不用冷启动），标准Elo公式互相更新。实现在 [mastery_model.py](测试外部agent服务/mastery_model.py)：`record_answer(student_id, question_id, correct)` 按Q矩阵展开、按知识点数量均分权重更新各知识点能力分（避免多标签题目重复计分占便宜），同时更新该题的难度分（整题只更新一次，用各知识点expected的均值）。已接入 [server.py](测试外部agent服务/server.py) 的 `POST /answer`（记一次作答）和 `GET /profile?student_id=xxx`（读取表B画像，对应之前规划的插件读接口），本地跑通验证（mastery_state按rating动态切换未学/学习中/薄弱/已掌握）
  - ⚠️ 待办：现在的表B持久化是本地JSON文件（[测试外部agent服务/data/](测试外部agent服务/data/)，已加入.gitignore不进版本库），模拟真表读写，字段/接口形状对齐9.5的表B schema——等3.4"自建HTTP API插件"分支决策落定、真正接入【资源管理→插件】时，把 `_load_json`/`_save_json` 换成调用真实插件存储即可，接口不用改
  - ⚠️ 待办：车道A/C的判断节点目前只输出judge/signal两个字段（见3.2/4.0），还没有在判断出对错后实际调用 `POST /answer`——这一步等4.1/4.3正式搭车道任务流时补上
  - **DKT思路（暂缓，列为二期路线）**：需要RNN在大量学生答题序列上训练，但3.1已验证"33个知识点掌握率全部0.0%"——目前没有真实数据可训练，且低代码任务流平台没有模型训练/部署基础设施。待表A/表B用CDM+Elo方案跑起来、积累几个月真实信号数据后，再考虑在[测试外部agent服务/server.py](测试外部agent服务/server.py)里离线训练DKT、定期批量刷新表B，作为MVP之后的优化项，不阻塞当前进度
- [x] **表B更新规则 v3：引入遗忘曲线，让"掌握度"随时间衰减，2026-09-16讨论确定路线并在外部服务里实现**：
  - **艾宾浩斯遗忘曲线简化公式（已实现）**：`R(t) = e^(-t/S)`，t = 距"最近更新时间"的天数，S = "记忆稳定性"，按掌握状态固定映射（薄弱=3天/学习中=7天/已掌握=21天，见 [mastery_model.py:STABILITY_DAYS_BY_STATE](测试外部agent服务/mastery_model.py)），不用像FSRS那样拟合参数。**不额外写回表B、不用加定时批量任务**——`_apply_forgetting_decay()` 在 `get_profile()` 读取时动态算，衰减方向是往"1500中性基准分"回退（越久没复习，越不确定还记不记得，而不是直接判定"忘光了"），持久化数据里的 raw Elo 分不受影响，跟现有"读时才算"的架构天然兼容。`GET /profile` 现在同时给 `raw_rating`（原始观测值）和 `rating`（衰减后现值）
  - **借用SM-2的"该不该提醒复习"间隔逻辑（已实现）**：新增 `get_due_for_review()`，从表B里挑出预估记忆保持率跌破阈值（`retention < 0.85`）的知识点，按掉得最狠的排前面，接到 [server.py](测试外部agent服务/server.py) 的 `GET /due_review?student_id=xxx`。本地跑通验证：把"最近更新时间"手动往前调20天，`retention` 从1.0掉到0.057，`needs_review` 正确翻转为true。下一步接【基础设置→触发器设置】周期触发——如每天定时调这个接口，判断有没有该提醒复习的知识点，触发车道C自测推送/5.2教师端学情预警，这一步还没接（等触发器联调阶段）
  - **FSRS/HLR/墨墨随机最短路径（暂缓，列为二期路线）**：三者都要用学生真实"复习-遗忘"历史数据拟合模型参数（非线性优化/回归训练/最短路径求解），跟DKT一样卡在"现在没数据、没训练infra"。待简化衰减公式跑起来、积累真实复习间隔数据后，再考虑升级

### ⚠️ 4.0.1 新发现：顶层路由不稳定 + 一个关键bug（2026-09-17，比原计划"完善触发关键词"更根本的问题）

> 背景：担心"学生二次对话时后续追问进不来，导致表A/B更新不到"，用【预览与调试】面板反复测试"测试-外部agent转发"任务流触发情况，发现的问题比预期严重，记录如下。

- **测试1：多轮对话的代词/指代消解本身没问题**——先问"槽轮机构是什么"，再追问"它是怎么运动的"（零关键词、纯代词），模型能正确关联回槽轮机构话题，运动过程讲解也对。**结论：不需要靠"每句话都带关键词"硬撑，多轮对话的语境保持是有效的。**
- **测试2（更严重）：顶层编排会不会调用我们的插件，不受任务流内部逻辑控制**——连续测试同一类问题（"压力角"×2次、"槽轮机构是什么"），观察到三种完全不同的处理路径随机出现：①调插件；②插件失败后自动降级去搜内置知识库（走教材原文RAG，不是我们的agent逻辑）；③什么都不调，模型直接凭自己的知识回答。这三条路径都会完全绕开`knowledge_base.py`分类、千帆讲解生成、`content_id`配图判断、表A信号记录——**学生这轮互动在我们自己的数据里等于"没发生过"**。这是黑盒机制，没有配置能强制"必须调用"。
  - **可用杠杆1（已验证有效）**：任务流卡片自身的"任务流描述"字段（600字符上限，在【任务流列表→操作→设置】里，不是画布里的节点，也不是【开始】节点的触发问题/相似问题）是顶层用来判断"要不要调用这条任务流"的关键信息之一。原描述只写了122字、只覆盖"平面连杆机构"（第三章），改写成覆盖全书26个知识点+"必须调用/不要用自己的知识回答/不要用知识库检索代替"这种强制性措辞（仿照内置"PPT生成"插件描述的写法）后，363字，**实测"槽轮机构是什么"（原本没命中过）成功调用了插件**。这个字段比【开始】节点的相似问题关键词列表更值得优先打磨。
  - **可用杠杆2（未测，用户提出的思路）**：顶层系统提示词（【基础调试→系统提示词】）本身能不能也加一条"任何机械原理/设计相关问题都必须调用XX工具"的强指令，作为比任务流自身描述更高权重的信号——值得下一步测试，目前还没验证效果。
  - **暂未找到的路数**：插件本身理论上能脱离任务流被顶层直接调用（参考内置"PPT生成"等插件的描述写法），但那样拿到的只是文本转发，接不上任务流里的【嵌入】节点，配图/3D这个核心亮点会丢——如果要保留视觉呈现，还是得走任务流链路。
- **测试3发现的真正bug（比路由不稳定更严重，2026-09-17已修复）**：任务流【插件】节点的`message`输入参数，从最早搭建/调试这条流程起，一直是**写死的固定字符串**"曲柄摇杆机构的3d动画演示一下"，从没换成动态变量引用。也就是说**不管学生实际问什么，转发给我们agent的永远是同一句话**——之前"压力角"/"曲柄摇杆...3D演示"这几次测试"看起来正常"，只是因为提问内容凑巧接近这个写死的字符串，是假阳性；真正问"槽轮机构是什么"时，插件返回的内容是风马牛不相及的"曲柄摇杆机构"讲解，连模型自己都在内部思考里察觉到"工具返回的好像还是曲柄摇杆的内容...可能是我调用出问题了？"。**修复**：把`message`的变量值从"输入"（手打固定值）模式切到"引用"模式，绑定到`开始-INNER_trigger_ques`（触发问题，即学生当前这句话），发布新版本（v0.0.8）后重测"槽轮机构是什么"，返回内容完全正确。**这个bug的教训**：以后任何一条任务流搭完调试通过后，除了确认"能跑通"，还要专门确认输入参数是不是引用了正确的动态变量，而不是习惯性地看到"返回了合理内容"就当作验证通过——静态测试值和动态引用在"用同一句话测试"时表现完全一样，区分不出来，必须换一个跟测试值不同的问题才能测出来。
- **对4.1及以后正式搭建的启示**：①正式车道搭建时任务流描述字段要按这次的写法覆盖全部知识点+强制调用措辞，不能沿用"仅测试用"的敷衍描述；②插件节点的输入参数绑定完成后，必须用至少2个内容差异明显的问题分别测试，不能只用1个问题就判定"通了"；③顶层路由的不确定性是架构级限制，接受不了100%必达的话，长期看应该考虑"定时批量拉取完整对话历史、事后补齐表A/B"作为兜底方案（插件列表里的"获取任务流外部历史消息记录"插件看起来是这个方向，还没细看，留作后续验证），而不是死磕"让每句话都实时命中插件"这一条路

### 4.1 车道A：报错求助

```
诊断题/学生描述报错 → 意图识别（对/错/部分对）
   → 错：读表B该知识点历史掌握度（已由4.0个性化层提供）
      → 若首次错：给提示，不跳视频
      → 若反复错：（如有视频）触发【应用库】视频跳转卡片；
        同时退回到该知识点的先修节点重新引导（如"整转副判断"反复错→退回"运动副与自由度"/
        "杆长条件"先修知识点，参照 [知识图谱.md](知识图谱.md) 的先修关系边）
   → 对：进入下一知识点或转车道C变式题验证
   → 出口：判断节点顺带输出signal字段写回表A（不再单独接信号提炼节点，见3.2/4.0）
```

> 难度分层思路参考 Claude for Teachers"按掌握度自动调整材料难度"的设计（见 [教学智能体方案讨论.md 8.1](教学智能体方案讨论.md)），比单纯重复同一提示更接近"退到最近发展区"的教学逻辑

- [ ] 按上述结构在【资源管理→任务流】里搭建车道A分支，覆盖6个知识点
- [ ] 【基础设置→触发器设置→添加触发器】如3.2验证通过，配置"反复答错同一知识点→推送视频卡片"的事件触发规则

### 4.2 车道B：主动学习（新，此前执行计划里完全没有对应任务流分支）

```
学生说"我想学第三章" / "我对XX不太懂" → 意图识别为"主动学习"
   → 判断颗粒度：章节级（"我想学第三章"）还是知识点级（"我对压力角不太懂"）
   → 读表B该学生在这些知识点的掌握状态（已由4.0个性化层提供）
      → 已掌握的知识点：跳过或只给一句话确认，不重复讲解
      → 未学/薄弱的知识点：按表B状态选讲解版本（阶段二准备的基础版/进阶版话术）
   → 讲完可顺势引导到车道D（"要不要拖一下看看效果"）或车道C（给1道诊断题验证）
   → 出口：判断节点顺带输出signal字段写回表A（不再单独接信号提炼节点，见3.2/4.0）
```

- [ ] 在【资源管理→任务流】新增车道B分支，意图识别提示词需要能区分"章节级 vs 知识点级"两种颗粒度的表达
- [ ] 讲解内容按阶段二准备的基础版/进阶版话术，用条件判断节点接表B状态做选择

### 4.3 车道C：自测复习（新，此前执行计划里完全没有对应任务流分支）

```
学生说"我要复习"/"考前自测" → 意图识别为"复习模式"
   → 读表B，按知识图谱先修顺序 + 薄弱知识点优先出题
   → 逐题记录对错（写入表A，作为"做题"类型信号，不再单独维护旧版"答题记录表"）
   → 生成结构化复习清单（四个设计动作·第4条：只在需要产出物时给模板）
   → 出口：判断节点顺带输出signal字段写回表A（不再单独接信号提炼节点，见3.2/4.0）
```

- [ ] 在【资源管理→任务流】新增车道C分支，出题顺序逻辑复用 [知识图谱.md](知识图谱.md) 的先修关系边
- [ ] 复习清单模板设计（结构化输出，覆盖"薄弱知识点+对应变式题+建议复习顺序"）

### 4.4 车道D：可视化探索（技术方案已实测确认，直接进入搭建）

- [ ] 【核心差异化亮点，必须实现，不可降级】**参数拖动式交互 MVP**：让学生拖动杆长滑块实时切换机构类型示意图（曲柄摇杆↔双曲柄↔双摇杆），建立几何直觉——这是纯对话式工具做不到的功能，是相对 Claude for Teachers 等同类产品的核心差异化优势（见 [教学智能体方案讨论.md 8.1](教学智能体方案讨论.md)）
  - **技术路线已实测确认（2026-08-21，登录 robot.chaoxing.com 后台实地验证）**：【资源管理→任务流→添加任务流】进入流程画布后，左侧节点面板"业务节点"分类下有一个**"嵌入"节点**，官方说明为"支持使用iframe地址和动态指令嵌入内容，当用户与 iframe 页面交互时才会产生输出参数"——这是**真正内嵌在对话流程里的iframe组件**，区别于【应用库】链接卡片（后者只是点击跳转/新开窗口，不算真嵌入）
  - 嵌入节点具体配置项：PC端/移动端 iframe地址（支持`{{变量名}}`动态拼接URL）、宽度(%)/高度(px)、动态指令回复（支持`{{变量名.子变量名}}`引用输出参数）、**输出**区（自定义变量名+变量类型，接收iframe页面通过`postMessage`回传的数据）
  - **落地步骤**：① fork 开源方案 [anupam-ayush/FourBarMechanismSimulation](https://github.com/anupam-ayush/FourBarMechanismSimulation)（纯HTML5 Canvas+原生JS，无框架无后端依赖，已支持输入四杆长度、自动识别机构类型、机架反演）② 在其JS里加几行`window.parent.postMessage(...)`逻辑，把学生拖动后的杆长比例/识别出的机构类型回传出来 ③ 部署成可公网访问的静态页面 ④ 把地址填进任务流"嵌入"节点的PC端iframe地址框，用"输出"变量接住数据，接入后续"意图识别/条件判断"节点，实现"拖动杆长→机构类型切换→自动判断对错/触发对应知识点讲解"的完整闭环
  - 现成的 GeoGebra applet（如 [Machine Dynamics 4 Bar Linkage](https://www.geogebra.org/m/BueCG9ch)）可以直接填进嵌入节点做**纯展示**用（零开发成本），但无法定制postMessage回传逻辑，拿不到"输出参数"——如果只是要一个能拖动看效果的演示，可以先用这个做过渡/兜底，但要做到"数据驱动后续任务流判断"这个完整亮点，仍需走上面的自建方案
- [ ] **新增（此前遗漏）：车道D 也要输出学习信号写回表A**——判断学生拖动行为是"有目的验证假设"还是"随意乱拖"，此前这条车道完全不产生学习数据，只做了"看效果"没有做"记效果"
- [ ] 【资源管理→应用库→添加应用】为已标注时间戳的知识点各建一张视频跳转卡片（依赖视频素材，若阶段二判定没有视频则跳过）

### 4.5 开放学习者模型（新，呼应 9.5"对学生透明"设计原则）

- [ ] 新增一个可被意图路由识别的分支："我学得怎么样了"/"我哪里还不会"，直接读表B把掌握度画像摊开讲给学生看，而不只是后台用——这条不算独立车道，是车道B的一种特殊触发方式，复用车道B的读表B逻辑，只是不进入讲解环节，直接呈现画像

### 4.6 联调验证

- [ ] 用右侧【预览与调试】完整走一遍学生视角，覆盖：车道A"一次答对/反复答错触发视频"、车道B"章节级/知识点级主动学习"、车道C"自测复习出清单"、车道D"拖动+数据回传"，以及跨车道场景（如车道A引导到车道D）

## 阶段五：教师端——按教师端能力地图收窄范围，只做两项已确认能力

**前置条件**：阶段四已上线一段时间，产生真实交互数据（表A/表B已有数据积累）

> 对应 [机械小助手_教师端能力地图.png](产品设计图/机械小助手_教师端能力地图.png)：教师端不是学生端的镜像，目前只确认两项能力，"教师主动查询"仍在讨论、**不纳入这版 MVP**

### 5.1 课堂演示（复用车道D，零额外开发）

- [ ] 教师打开同一入口，触发车道D同款嵌入组件，拖动+讲解流程与学生端完全一致
- [ ] 区别于学生端：演示模式下跳过表A/B的信号写入，不产生学生个人档案数据（避免教师操作污染学生画像）

### 5.2 学情画像自动推送（触发器+Skill定时任务）

- [ ] 【数据统计→问答统计→高频问题统计】导出班级高频问题，人工标注对应知识点（作为表A自动化统计之外的补充校验）
- [ ] 【数据统计→问答统计→重点问题分析】查看平台自动识别的重点问题，与表A/表B里的信号数据对照
- [ ] 【资源管理→智能学习→不满意问题】筛选出学生标记"不满意"的回答，定位是哪个知识点的讲解需要改进，反哺【问答库】/【知识库】内容迭代
- [ ] 调用【Skills→data-analysis】或【Skills→echarts-generator】，把表A/表B数据喂给它，生成班级学情雷达图或知识点掌握度柱状图
- [ ] 【差异化亮点】用【基础设置→触发器设置】的"时间触发"，把"读表A/B→生成班级画像"串成定时任务（如每周固定时间自动跑一遍，推给老师），而非依赖老师手动点按钮查看——借鉴 Claude for Teachers 用 Cowork 定时自动跑重复工作的思路（见 [教学智能体方案讨论.md 8.1](教学智能体方案讨论.md)），工作量不大（复用已验证的Skill+已有的表A/B），但多数同类参赛作品大概率不会做到这一层
- [ ] **推送方式已找到**（2026-08-23确认，见3.5）：定时任务流跑完画像生成后，末尾接【Skills→notice-push】节点按UID推给张老师本人（比【Skills→泛雅发送通知】的班级群发更精准），不需要老师主动登录后台查看，真正做到"自动推送"而不是"自动生成等着被看"
- [ ] （可选，若拿到导出中心权限）【导出中心】批量导出统计数据用于制作参赛现场汇报材料

### 5.3 明确排除在本版 MVP 之外

- **教师主动查询**（如"帮我看看这道题全班答错率"）：产品设计图上标为"讨论中"，本版执行计划不排期，待 9.4 的待确认项定下来后再补

## 阶段六：接入与提交

> **截止时间（2026-09-17更正）：2026年10月10日17:00**，官方通知原文见 [首届教学智能体大赛通知.pdf](首届教学智能体大赛通知.pdf)。

- [ ] 【接入渠道→网站→体验Web智能体】取新版链接 `robot.chaoxing.com/coze?unitId=2339&robotId=...`，点"生成短链"得到便于分享的地址
- [ ] 回到智雅【项目管理】，用"第三方链接创建"把这个体验链接登记进智雅项目卡片（名称/描述/链接/分类/图标/封面）
- [ ] **教学智能体展示视频（6-10分钟/MP4/720P+/≤1200M）需要提前录制、剪辑好**——这份材料跟申报书/建设说明书一起是网络评审阶段就要交的，不是现场评审才要，且要过一遍"不得透露参赛教师姓名/学校/院系"的匿名审查
- [ ] 回到大赛官网【我的作品】模块，按流程提交：申报书（含三处学校盖章）→ 建设说明书（简介≤1200字+应用成效≤1800字，在官网直接填写）→ 展示视频 → 测试链接/二维码/测试账号 → 原创性证明及佐证材料（需教务盖章）
- [ ] 截止时间前可反复点"暂存"修改；若已点"提交"，截止前仍可"编辑"，**申报书本身提交后原则上不得修改**（PDF原文），建议申报书留到其他材料都确认得差不多了再最终提交

---

## 依赖清单

### 必须先有 / 先确定的外部条件

| 依赖项 | 说明 | 阻塞的阶段 |
|---|---|---|
| **课程是否已在学习通/泛雅建课，且账号已关联** | "课程学情分析""泛雅课程信息""泛雅作业管理""知识点双率统计"这一整套官方能力，大概率要求当前 robot.chaoxing.com 账号背后关联的教师账号，在学习通侧已经开课、有学生、有知识点体系，否则调用只会拿到空数据——这是阶段三验证最先要确认的事 | 阶段三、四、五 |
| **知识点标准命名/知识点库** | "知识点双率统计"插件按"知识点名称"查询，需要跟学习通后台已建的知识点体系命名完全一致，我们阶段二自定义的6个知识点名字要跟学习通那边对齐，或者反过来在学习通建知识点库 | 阶段三、四 |
| **教学视频素材** | 目前完全没有。没有视频，阶段三的"应用库时间戳跳转"验证和车道A对应搭建环节都要跳过，先做纯图文引导版本 | 阶段二、三、四 |
| **视频托管方式** | 如果后续有视频，要先定是自建服务器/学习通视频库/B站等第三方平台——这决定"应用库"链接卡片里时间戳参数的具体写法 | 阶段三、四 |
| **导出中心权限** | 当前账号 403 无权限，如果阶段五需要批量导出数据做参赛汇报材料，要单独申请或找有权限的人协助 | 阶段五（非必须，锦上添花） |
| **报名表单具体选项内容** | "赛道类别/智能体类别/应用场景"下拉框内容还没实际看清（手册PDF是图片），需要登录后台确认，可能反过来影响我们对项目定位的文案表述 | 阶段一、六 |
| **团队分工** | 若团队报名，内容设计（题目/知识点/两版讲解话术）、平台搭建（四条车道任务流+记忆系统表A/B）、视频制作（如果做）三块工作谁负责，需要提前定，阶段三、四工作量较大建议至少2人 | 全部阶段 |
| **学校统一身份认证入口地址** | 2026-08-23实测确认【访客端设置→访客登录→第三方认证地址】需要学校IT部门提供的统一认证URL（类似CAS单点登录），不是项目组能自己配出来的，需要单独联系学校信息化部门要 | 阶段三、四（学生免登录对接） |
| ~~**表A/表B的外部后端托管**~~ | 首选方案【插件】自建HTTP API已2026-09-15~16实测端到端成功。但这次用的是本机+cloudflared临时隧道，正式使用前仍需换成稳定托管（Railway/Render等，固定域名，不依赖本机常开） | 阶段四 |
| **⚠️ TODO：server.py配图HTTPS化（进行中，卡在Cloudflare账单信息）** | 2026-09-22发现：robot.chaoxing.com是https页面，`server.py`只有http（`http://106.14.140.27:8899`），浏览器对https页面里的http iframe/图片做混合内容(mixed content)静默拦截——抓包确认浏览器对`/viewer`、`/media`这类地址一次网络请求都没发出去，这基本解释了"配图经常空白/一直转圈"这个反复出现的现象，很可能不是今天才有的问题。解法：买域名+接入Cloudflare+服务器装持久化`cloudflared`建tunnel（域名解析走Cloudflare的IP、服务器只是主动连出去，不需要ICP备案）。**当前进度**：域名`jixiexiaozhushou.cn`已购买（¥38/年）并把DNS服务器改成了Cloudflare的`leo.ns.cloudflare.com`/`ollie.ns.cloudflare.com`（等待生效）；Cloudflare账号已注册；服务器上`cloudflared`已装好（2026.9.1，通过scp绕开了服务器到GitHub的连接问题）。**卡点**：Cloudflare的Tunnels功能在Zero Trust下面，账号要先完成"Activate Zero Trust Free"这一步，这一步需要填账单信息（哪怕是$0套餐也要),涉及支付信息不能代填，需要用户自己去Cloudflare dashboard完成。完成后继续：创建tunnel → 配置ingress指向localhost:8899 → 在Cloudflare DNS里加子域名的CNAME记录 → 改`server.py`对外URL和robot.chaoxing.com任务流【嵌入】节点里写死的`http://106.14.140.27:8899/...`模板 → 重新验证配图真的能显示 | 阶段四（4.0公共基础设施） |

### 技术不确定性（需实测才知道，不是缺资源，是不确定平台能力边界）

| 不确定点 | 对应验证步骤 | 如果不行的备选方案 |
|---|---|---|
| 官方Skill/插件的数据颗粒度能否精确到"单个学生+单个知识点" | 阶段3.1 | 完全依赖自建的表A/表B，用任务流变量手动记录 |
| 任务流"大模型意图识别"能否区分细粒度错误类型（不只是对/错） | 阶段3.2 | 在系统提示词里补充更细的判断规则，或拆分成更多层级的子任务流 |
| "信号提炼"节点能否合并进已有的"意图识别"节点一起输出 | 已实测，见阶段3.2 | **JSON合并输出机制本身可行**（同一次调用输出judge+signal，2.58s/258 tokens），但**泛化测试发现更严重的问题没解决**：①裸模型判断专业对错不可靠，测出真实的假阴性（教科书正确答案被判"概念混淆"）；②意图路由脆弱，"我不会/没学过"这类真实回答可能完全无法触发进入任务流。4.1正式搭车道A时要专门解决这两点，不能只当作"已解决"直接照抄 |
| 【应用库】链接卡片是否支持视频秒级时间戳跳转 | 阶段3.2 | 改用"点击后展示该知识点对应视频片段的文字版讲解摘要"退而求其次 |
| ~~触发器"事件触发"是否支持"重复行为"类条件~~ | 已实测证伪，见阶段3.2 | **不支持**——"事件触发"只有Webhook(Catch hook)一种模式，是外部系统调用平台的入站接口，不是平台监听内部行为的枚举事件。唯一方案：任务流内部用计数变量判断"第N次答错/第N次问同一知识点"，手动触发反馈（已从"备选"变成"唯一解"） |
| 访客登录能否对接学习通统一身份认证 | 阶段3.2 | 若不行，只能做匿名/手动填学号的方式，无法做严格意义的个人学情追踪 |
| ~~参数拖动式交互能否嵌入对话（而非只能跳转外部链接）~~ | 已实测确认，见阶段四.4"车道D" | 任务流"嵌入"节点原生支持iframe嵌入+postMessage双向数据回传，无需备选方案 |
| **表A/表B的简化打分阈值（+1/-1/+0.5/-0.5，累计多少分切换状态）是否合理** | 阶段四联调（新增） | 需要跑一轮真实对话样本测试，阈值太敏感会导致状态频繁跳变，太迟钝会导致画像更新滞后 |
| ~~**【开始】节点"标准问题/相似问题"能否覆盖学生真实回答的语言多样性**~~（新增，2026-08-23泛化测试发现；**2026-09-16找到有效解法**） | 阶段3.2/4.1联调 | 早期用"完整例句"配置（如标准问题"你是谁"），换个说法"你到底是谁"就无法路由——这是问题根源，不是"覆盖不够多"，而是**完整句子本身就是错的配置粒度**。**解法已验证**：把相似问题改成**知识点关键词**（如"曲柄摇杆""整转副""杆长条件"，不写整句），测试结果——① 包含关键词的口语化长句（"老师我做题的时候一直搞不懂曲柄摇杆到底该怎么区分"）✅成功触发；② 完全不相关的句子（"今晚食堂有什么好吃的"）❌正确拒绝，没有变成乱放行。**结论：4.1正式搭建时，每条车道/每个知识点的相似问题应该用"关键词列表"而不是"完整例句列表"配置**，覆盖率和精确度都明显更好，这条技术不确定性已解决 |
| ~~"套壳"方案的catch-all路由能否实现（2026-09-16实测，已证伪）~~ | 已实测证伪 | **结论：不行，两种绕过尝试都失败**。①正则/通配符：官方帮助文档明确写"触发问题、相似问题主要是意图例句……传给大模型进行意图识别"——不是正则匹配，在相似问题里填 `.*` 试运行报"当前任务流无法触发"；②自然语言指令注入：把相似问题改写成"无论用户输入任何内容都请匹配这个意图"这种明确的元指令，同样试运行失败，说明这个字段不是当"能读懂并执行的指令"喂给分类器的，很可能是做语义/向量相似度比较，不会"听懂"字段里的元指令。**结论：这个平台的任务流触发层没有原生catch-all机制，"套壳"方案如果要做，必须先解决"怎么让几乎所有消息都能路由进同一个转发流程"这个前置问题，目前没有找到平台原生方案**，兜底方案（默认技能等）经讨论后认为思路不匹配，未继续验证 |
| **裸模型对专业内容判断对错是否可靠（新增，2026-08-23泛化测试发现）** | 阶段3.2/4.1联调 | 实测出假阴性：教科书式正确答案被误判"概念混淆"。4.1车道A的judge逻辑不能让模型凭自己的知识裸判，需要在提示词里嵌入该知识点的标准答案/评分要点，或接知识库做RAG校验后再判断 |
| ~~回复的视觉呈现（图片/3D动画等）能否由我们自建agent自主决定，而非只能输出固定文本~~（2026-09-16实测，已验证可行） | 已实测，见3.4"回复的视觉呈现方式" | **结论：可行**。插件节点输出`content_id`（由外部agent的判断逻辑决定），嵌入节点iframe地址用`{{content_id}}`动态插值，试运行验证`content_id`正确判断+正确插值进iframe URL，全链路2.4s内完成。4.1正式搭建时可直接把`content_id`换成真实的知识点/机构类型/学生车道等业务判断结果 |

---

## 建议的优先顺序

1. **先做阶段三 3.1 + 3.2**（含"信号提炼节点能否合并进意图识别节点"验证）——这仍是最大的分支点，决定表A/B多大程度依赖官方数据、以及记忆系统的大模型调用成本是否可控。**2026-08-23泛化测试后发现两个必须在4.1之前解决的子问题**：判断准确率（裸模型判专业对错有假阴性，仍未解决）、~~路由覆盖率（"我不会"类真实回答可能完全进不了任务流）~~——**路由覆盖率已于2026-09-16找到解法：相似问题改用知识点关键词而非完整例句配置**，见技术不确定性表，4.1可以直接采用这个配置方式
2. 并行推进**阶段二**内容准备（不依赖平台，随时能做，且不管技术验证结果如何都用得上；两版讲解话术是新增工作量，优先给车道A已有的6个知识点补齐）
3. **阶段四先搭 4.0 公共基础设施（意图路由+个性化层+表A/B）**，这是四条车道共用的骨架，不要先扎进某一条车道细节；信号提炼不再是单独节点，随各车道判断节点一起搭（2026-08-23更新）
4. 4.0 搭完后，**车道A + 车道D 优先**（车道A已有较完整设计基础，车道D技术方案已实测确认），车道B/C 是此前完全空白的新分支，放在两者之后
5. 车道A/D 跑通并接上信号提炼节点后，再横向扩展车道B/C，最后扩展到全部6个知识点，不要一次性全量搭建
6. 阶段五教师端两项能力（课堂演示零额外开发，学情画像推送依赖表A/B已有数据）放在阶段四主体跑通之后
7. 报名（阶段一）时间不紧张（截止9.21），建议放在阶段三验证完、大致确定项目最终形态之后再定文案，避免申报材料和实际做出来的东西对不上
