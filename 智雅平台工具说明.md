# 智雅（教学智能体创作平台）功能说明

> 平台地址：https://zhiya.chaoxing.com/fable-ans-vue/personalWorkbench/
> 记录时间：2026-08-07（登录后实测 + 官方使用说明文档整理）
> 这是"首届教学智能体大赛"官方提供的智能体开发/管理平台（此前一直没找到入口，登录后确认就是它）

## 一、平台整体结构

登录后【个人工作台】下分两大模块：

- **项目管理** —— 创建/管理智能体应用本身
- **权限管理** —— 管理智雅平台数据对第三方平台（如 Coze/扣子）的开放授权，**不是创建智能体的地方**

## 二、项目管理 —— 创建智能体的两种方式

点击"新建项目"，弹出"创建AI应用"，可选：

### 1. AI创建
- 一个文本框"输入主题生成应用"，点击"生成"，由平台AI直接零代码生成一个智能体应用
- 适合快速生成 Demo / 低代码场景，实际生成效果未验证（未点击生成，避免占用额度）

### 2. 第三方链接创建
- 表单字段：**名称、描述、链接、分类、图标（上传/AI生成）、封面（上传/AI生成）**
- 本质是把**已经在别的平台开发好的智能体**（比如 Coze/扣子、讯飞星火等做的智能体，拿到一个体验链接）登记接入智雅平台展示，智雅本身不承载真正的开发过程
- **推论**：如果我们的方案技术复杂度较高（比如结合知识追踪算法、视频时间戳跳转等），很可能需要在 Coze 等外部平台真正开发智能体，再通过这个入口把体验链接接入智雅完成报名/展示

## 三、权限管理 —— 第三方数据授权（不是开发智能体，是"数据接入"）

### 3.1 功能定位（来自官方使用说明）
"智雅权限管理平台-第三方授权管理"功能，是把**智雅平台的数据能力**（比如学生课程信息、学习进度/成绩、AI学情分析数据）通过 **OAuth 授权机制**，开放给 Coze（扣子）等第三方智能体开发平台使用。

即：**你在 Coze 里搭智能体，需要读取智雅平台的学情数据时，先在智雅这边生成密钥/OAuth凭证，再配置到 Coze 插件里，才能让 Coze 智能体调用智雅的数据。**

这与"用户猜测的'从第三方MCP拿数据'"一致——只是方向反过来：**是第三方平台（如Coze）通过智雅开放的接口/MCP来读取智雅侧的数据**，密钥/授权是给"第三方平台访问智雅数据"用的，不是"智雅访问第三方MCP数据"。

### 3.2 新建密钥流程（三步）
1. **基本信息**：名称、说明、选择接口（选中要开放哪些数据接口，⚠️ 提交后不能改，需要重开）
2. **凭证信息**：生成 Scope（数据范围）、Client ID、Client Secret（仅显示一次）、Authorization URL、Token URL
3. **配置回调**：将 Coze 侧生成的回调地址（格式 `https://www.coze.cn/api/plugin_oauth/{插件ID}/authorization_code`）填回智雅

### 3.3 已知可开放的数据接口（当前"接口查询"页实测只看到 1 个）
| 方法 | 名称 | 路径 |
|---|---|---|
| POST | AI学情分析数据 | `/openai/studyai/data` |

**这个接口和我们此前设计的"学情诊断+效果评估"方案高度吻合**，如果内容够用，可以直接调用平台自带的学情数据，不必自己从零采集学生数据。

### 3.4 安全提示（来自官方文档）
- Client Secret 仅生成时完整显示一次，需妥善保管，不能发到群聊/截图/给无关人员
- 泄露后可在"密钥管理→重置密钥"，重置后原密钥立即失效需同步更新 Coze 侧配置

## 四、对接 Coze（扣子）完整流程（官方使用说明摘要）

```
智雅：新建密钥，选接口，生成OAuth凭证
   ↓
Coze：资源库→新建插件（云端插件 或 MCP 均可）→授权方式选OAuth(standard)→填入智雅给的凭证
   ↓
Coze：拿到插件ID → 生成回调地址 https://www.coze.cn/api/plugin_oauth/{插件ID}/authorization_code
   ↓
智雅：第三方授权管理→找到对应记录→"继续配置"→粘贴回调地址→完成
   ↓
用户首次使用智能体相关功能时，会跳转智雅授权页确认授权，之后智能体才能实际读到数据
```

授权状态三种：**待配置**（缺回调地址）/ **启用中**（可用）/ **未授权**（已被取消）

## 五、对我们方案的启示

1. **智能体的实际开发大概率不在智雅平台内完成**（智雅更像"注册台+数据授权中心"），真正搭建逻辑推荐去 **Coze（扣子）** 等平台做（官方文档专门写了对接 Coze 的教程，说明这是官方推荐路径）
2. 智雅自带的 **`/openai/studyai/data`（AI学情分析数据）接口**，可以作为我们"学情诊断"模块的数据来源之一，不用完全自建，需要进一步确认这个接口具体返回什么字段（暂未看到接口详情/请求参数）
3. "第三方链接创建"入口说明：最终提交作品形式很可能是**在智雅登记一个体验链接**，指向我们在 Coze（或其他平台）搭好的智能体

## 六、待确认事项

- [ ] `/openai/studyai/data` 接口的具体请求参数和返回字段（接口查询页里"复制接口"按钮点开可能有详情，未展开查看）
- [ ] "AI创建"生成的应用实际效果、是否可导出/迁移（未实际点击生成测试）
- [ ] 报名流程里最终提交的"智能体体验链接"，具体应该填智雅项目链接还是 Coze 智能体链接（需结合报名手册确认）
